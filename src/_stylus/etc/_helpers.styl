.wp
  min-height: 100%
  min-width: $wpMinWidth
  display: flex
  flex-direction: column
  align-items: stretch
  overflow: hidden
  position: relative
  &__content
    flex-grow: 1
    padding-bottom: 30px

.container
  max-width: $wpMaxWidth
  min-width: $wpMinWidth
  margin: 0 auto
  padding: 0 20px
